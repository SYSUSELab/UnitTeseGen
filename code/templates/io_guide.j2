Write a JUnit5 test class for the focal method in the target class using JUnit5 and Mockito libraries.

Guidelines:
1. Analyze the method's functionality and identify possible input combinations including normal cases and boundary conditions.
2. Follow existing test class structure, and write test methods for uncovered input combinations.
3. Reinitialize the variables in the @Setup function if needed for specific test cases

Test Class Requirements:
- Clear and maintainable code
- Avoid direct access to private methods/fields
- Use parameterized tests when appropriate
- Add necessary import statements

The details are as follows:

@input{focused method and target class}
Here is the code of focal method, `{{ method_name }}`, with the focal class `{{ class_name }}`:
```Java
{{ class_code }}
```

@input{existing test class}
```Java
<initial_class>
```

{% if context_dict | length > 0 %}
@input{context information}
{% for key, value in context_dict.items() %}
{{ key }}: 
{{ value }}
{% endfor %}
{% endif %}

@output{test class}: output the code wrapped in a Markdown code block ```java at the beginning and ``` at the end.